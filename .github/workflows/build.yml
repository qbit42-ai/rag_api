name: build 

on:
  workflow_call:

jobs:

  build:
    uses: qbit42-ai/aws-management/.github/workflows/_build.yml@main
    permissions:
      id-token: write
      contents: read
    secrets: inherit
    with:
      ENVIRONMENT: shared-services
      AWS_REGION: eu-central-1
      REPO: qbit42-rag-api